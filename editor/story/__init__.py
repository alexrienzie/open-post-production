"""Story layer: sidecar pipeline + the EP resources.

`_sidecar scripts/` turn the canonical XML cut into Act-scoped sidecars
(spec: `sidecars/sidecars_README.md`). `_resources/` holds the story-structure
frameworks (macro) and craft principles (micro) that load via `resources.py`.
See `editor_story_README.md`.
"""
